# Copy to .env — auto-loaded.
BROWSER_USE_API_KEY=bu_your_key_here

# Optional: restrict local browser discovery to one browser family.
# Use chrome when the agent must never attach to Brave/Edge.
BH_BROWSER_FAMILY=chrome
